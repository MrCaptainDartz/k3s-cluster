# No `namespace:` and no cluster-settings component: every object names its own namespace, no $(VAR).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - serviceaccount.yaml
  - rbac.yaml
  - token-secret.yaml
