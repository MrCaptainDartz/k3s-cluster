---
# OVH DNS-01 webhook for cert-manager (HelmChart CR); deployed when certmanager_enabled + OVH cred exists. groupName MUST match 24- issuers.
apiVersion: helm.cattle.io/v1
kind: HelmChart
metadata:
  name: cert-manager-webhook-ovh
  namespace: kube-system
spec:
  repo: "{{ certmanager_webhook_ovh_chart_repo }}"
  chart: cert-manager-webhook-ovh
  version: "{{ certmanager_webhook_ovh_chart_version }}"
  targetNamespace: cert-manager
  valuesContent: |-
    groupName: {{ certmanager_webhook_group_ovh }}
    certManager:
      namespace: cert-manager
      serviceAccountName: cert-manager