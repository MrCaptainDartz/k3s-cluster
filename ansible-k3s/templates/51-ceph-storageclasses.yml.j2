---
# RBD (block) StorageClass — cluster default. reclaimPolicy: Delete (block PVCs are ephemeral app data; reclaims Ceph image, keeps pool tidy).
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: "{{ ceph_sc_rbd_name }}"
  annotations:
    storageclass.kubernetes.io/is-default-class: "true"
provisioner: rbd.csi.ceph.com
parameters:
  clusterID: "storage"
  pool: "{{ ceph_rbd_pool }}"
  imageFeatures: layering
  csi.storage.k8s.io/provisioner-secret-name: csi-rbd-secret
  csi.storage.k8s.io/provisioner-secret-namespace: ceph-csi-operator-system
  csi.storage.k8s.io/controller-expand-secret-name: csi-rbd-secret
  csi.storage.k8s.io/controller-expand-secret-namespace: ceph-csi-operator-system
  csi.storage.k8s.io/node-stage-secret-name: csi-rbd-secret
  csi.storage.k8s.io/node-stage-secret-namespace: ceph-csi-operator-system
reclaimPolicy: Delete
allowVolumeExpansion: true
mountOptions:
  - discard
---
# RBD (block) StorageClass — Retain variant for data that must NOT be lost on PVC deletion (DBs, app data
# you can't regenerate). No default annotation -> opt-in by explicit storageClassName. Ceph image is kept
# on PVC delete (PV -> Released); reclaim the backing image manually once (rbd rm) — see table-rase notes.
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: "{{ ceph_sc_rbd_retain_name }}"
provisioner: rbd.csi.ceph.com
parameters:
  clusterID: "storage"
  pool: "{{ ceph_rbd_pool }}"
  imageFeatures: layering
  csi.storage.k8s.io/provisioner-secret-name: csi-rbd-secret
  csi.storage.k8s.io/provisioner-secret-namespace: ceph-csi-operator-system
  csi.storage.k8s.io/controller-expand-secret-name: csi-rbd-secret
  csi.storage.k8s.io/controller-expand-secret-namespace: ceph-csi-operator-system
  csi.storage.k8s.io/node-stage-secret-name: csi-rbd-secret
  csi.storage.k8s.io/node-stage-secret-namespace: ceph-csi-operator-system
reclaimPolicy: Retain
allowVolumeExpansion: true
mountOptions:
  - discard
---
# CephFS (shared FS) StorageClass. reclaimPolicy: Retain: durable data survives accidental PVC deletion (subvolume -> Released PV, re-importable); cost is manual cleanup of orphans.
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: "{{ ceph_sc_cephfs_name }}"
provisioner: cephfs.csi.ceph.com
parameters:
  clusterID: "storage"
  fsName: "{{ cephfs_fs_name }}"
  csi.storage.k8s.io/provisioner-secret-name: csi-cephfs-secret
  csi.storage.k8s.io/provisioner-secret-namespace: ceph-csi-operator-system
  csi.storage.k8s.io/controller-expand-secret-name: csi-cephfs-secret
  csi.storage.k8s.io/controller-expand-secret-namespace: ceph-csi-operator-system
  csi.storage.k8s.io/node-stage-secret-name: csi-cephfs-secret
  csi.storage.k8s.io/node-stage-secret-namespace: ceph-csi-operator-system
reclaimPolicy: Retain
allowVolumeExpansion: true
